<nav class="navbar">
  <div class="navbar-container">
    <div class="nav-left">
      <!-- Menu button for mobile -->
      <button class="hamburger" aria-label="Toggle navigation">
        ☰
      </button>

      <!-- Navigation Links -->
      <ul class="nav-links">
        <li><a href="<%= root_path %>"><%= t('nav.home') %></a></li>
        <li><a href="<%= agenda_index_path %>"><%= t('nav.agenda') %></a></li>
      </ul>
    </div>

    <!-- Language Switcher on the Right -->
    <div class="language-switcher">
      <%= link_to "EN", url_for(locale: :en), data: { turbo_frame: "_top" }, class: "#{I18n.locale == :en ? 'active' : ''}" %> |
      <%= link_to "DE", url_for(locale: :de), data: { turbo_frame: "_top" }, class: "#{I18n.locale == :de ? 'active' : ''}" %>
    </div>
  </div>
</nav>
